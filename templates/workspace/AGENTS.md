# Workspace Instructions

This workspace was bootstrapped by `MultiPlus`.

## Rules

1. Keep `.codex-home/` local and uncommitted.
2. Prefer `./multiplus-local.sh` so each run uses the workspace-local Codex home.
3. Add project-specific instructions here rather than editing generated profile homes.
4. Treat auth, sessions, logs, and caches as local machine state.
